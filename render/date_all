set -eu -o pipefail
export TZ="America/Los_Angeles"
cd "$(dirname "$0")/.."

git ls-files -- '*.md' ':!*/archived/*' ':!*README.md' \
| xargs -d '\n' -I{} git log -1 --format="%cd {}" --date=format:"%Y-%m-%d %H:%M:%S" -- {}\
| sort -nr